mutation RemoveItemFromCart($productId: Int!) {
  removeItemFromCart(productId: $productId) {
    success
    message
    cart {
      id
      items {
        productId
        name
        price
        quantity
        product {
          id
          name
          description
          discountedPrice
        }
      }
      totalAmount
    }
  }
}
